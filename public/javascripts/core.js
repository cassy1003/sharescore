// Generated by CoffeeScript 1.3.3
var __slice = [].slice;

window.namespace = function(target, name, block) {
  var item, top, _i, _len, _ref, _ref1;
  if (arguments.length < 3) {
    _ref = [(typeof exports !== "undefined" && exports !== null ? exports : window)].concat(__slice.call(arguments)), target = _ref[0], name = _ref[1], block = _ref[2];
  }
  top = target;
  _ref1 = name.split('.');
  for (_i = 0, _len = _ref1.length; _i < _len; _i++) {
    item = _ref1[_i];
    target = target[item] || (target[item] = {});
  }
  return block(target, top);
};

window.use = function(ns) {
  return namespace(ns, function(exports) {
    return exports;
  });
};

namespace('wrap.ajax', function(exports) {
  var _cache, _type, _url;
  _type = 'GET';
  _cache = false;
  _url = 'http://mydic.cassy.jp.net/server';
  return exports.send = function(data, type, cache, url) {
    if (type == null) {
      type = _type;
    }
    if (cache == null) {
      cache = _cache;
    }
    if (url == null) {
      url = _url;
    }
    return $.Deferred(function(d) {
      return $.ajax({
        type: type,
        url: url,
        data: data,
        cache: cache,
        success: function(result) {
          return d.resolve(result);
        }
      });
    });
  };
});
